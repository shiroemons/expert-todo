= form_with(local: true, url: login_path) do |f|
  .field
    = f.label :email
    = f.email_field :email, required: true
  .field
    = f.label :password
    = f.password_field :password, required: true
  .actions
    = f.submit 'Login'
